# ADR-009: 引入 PostToolUse hook subagent 调用追踪机制，确保 commit 前必须经过 subagent 审计

- **Date**: 2026-05-03
- **Status**: Proposed
- **Deciders**: arcaneorion
- **Tags**: 

## Context

<!--
描述起作用的力量:技术的、政治的、社会的、项目局部的。
这些力量往往处于紧张状态,应明确指出。
语言保持价值中立,只陈述事实和约束,不预判决策方向。
-->

## Decision

<!--
我们将...(主动语态,完整句子)
只写最终选择,不写"可能"、"或许"。
-->

## Consequences

<!--
- 积极后果:
- 消极后果:
- 中性后果(被引入的新约束 / 新的学习成本 / 路径依赖):
-->

## Alternatives Considered

<!--
列出被排除的备选方案及其排除理由。
这部分对未来读者最有价值——帮他们理解决策边界。
-->

## Implementation Plans

- **Plan**: doc/arch/plans/ADR-009-plan-001.md
- **Summary**: ADR-009 实施完成：移除 hash chain token，引入 PostToolUse hook (Agent matcher) 记录 subagent 调用到 subagent-sessions.log，commit-msg hook 校验链升级为 token→subagent session→ADR。修复 3 个 bug：cmd_hook_post_tool_use 重复定义、_init_claude_hook 只匹配 Bash、旧 stdin-JSON 测试过时。107 pytest 全通过。
- **Pre-Validation**: 基线 107 pytest 全部通过。当前状态：ADR-008 hash chain token 机制已实现但存在哲学缺陷——AI 主 agent 同时控制 report 写入端和 submit 校验端。ADR-009 将用 PostToolUse hook (harness 层，AI Bash 工具无法干预) 替代 hash chain 作为 subagent 审计的证据来源。Spec-004 已创建并就绪。待修改文件：commit.py (移除 hash chain，还原 basic token，新增 subagent session 检查)、cli.py (移除 hash chain 代码，新增 post-tool-use 和 prepare-ts)、hooks.py (新增 run_post_tool_use)、settings.json (注册 PostToolUse hook)、SKILL.md (更新 commit 段)。
- **Post-Validation**: 修改后验证：(1) 全量 107 pytest 通过。(2) write_commit_token 还原为 2 行 basic token 格式（uuid+timestamp），hash chain（5 行、SHA-256、哈希比对）完全移除。(3) validate_and_consume_token 新增 subagent session 检查：.git/spec-vc-subagent-sessions.log 缺失时阻塞输出'未找到 subagent 审计记录'。(4) spec-vc hook post-tool-use --tool-name Agent --description '...' 正确追加日志行到 .git/spec-vc-subagent-sessions.log，格式为 ISO 时间戳 | Agent | description。(5) SPEC_VC_BYPASS 设置后跳过 token + subagent session 检查直接放行。(6) cmd_commit_prepare 新增写入 .git/spec-vc-prepare-ts，cmd_commit_submit 移除哈希计算和 hash chain 相关代码。(7) SKILL.md commit 段已更新为 subagent session 检查 + PostToolUse hook 配置说明。(8) test_cli.py hash chain 测试替换为 subagent session 测试（含 session 检查通过、无 session 阻塞、bypass 跳过）。
## References

- **Related ADRs**: <!-- ADR-XXX (supersedes / is superseded by / relates to) -->
- **Commits**: cc4f9b6 feat(security): 引入 PostToolUse hook 替代 hash chain token 作为 subagent 审计证据 [ADR-009]
- **Specs**: <!-- 未来由 spec-vc 填充:引用 specs/ 下的契约单元 + 版本 -->
- **External**: <!-- 论文、RFC、issue、讨论链接 -->

---

<!--
Status 状态机:
  Proposed        ← 初始,提交后等待讨论
  Accepted        ← 决策生效,代码可开始实现
  Deprecated      ← 不再使用但未被替代
  Superseded by ADR-XXX  ← 被新决策替代,保留历史

更改状态时不要删除旧记录,在本文件顶部 Status 字段更新即可。
-->
