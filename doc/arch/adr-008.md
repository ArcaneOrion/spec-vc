# ADR-008: 引入 TTY-bound commit 机制，将 spec-vc commit 拆分为 prepare/submit 两阶段

- **Date**: 2026-05-03
- **Status**: Proposed
- **Deciders**: arcaneorion
- **Tags**: 

## Context

<!--
描述起作用的力量:技术的、政治的、社会的、项目局部的。
这些力量往往处于紧张状态,应明确指出。
语言保持价值中立,只陈述事实和约束,不预判决策方向。
-->

## Decision

<!--
我们将...(主动语态,完整句子)
只写最终选择,不写"可能"、"或许"。
-->

## Consequences

<!--
- 积极后果:
- 消极后果:
- 中性后果(被引入的新约束 / 新的学习成本 / 路径依赖):
-->

## Alternatives Considered

<!--
列出被排除的备选方案及其排除理由。
这部分对未来读者最有价值——帮他们理解决策边界。
-->

## Implementation Plans

- **Plan**: doc/arch/plans/ADR-008-plan-001.md
- **Summary**: ADR-008 实现完成：将 spec-vc commit 拆分为 prepare/submit 两阶段，token 仅在用户 TTY 运行的 submit 阶段写入（含 hash chain），AI 无法获取提交权限。修改涉及 commit.py（hash chain token + validate 升级）、cli.py（prepare/submit 命令）、SKILL.md（commit 段重写）、CLAUDE.md/README.md（文档更新）。新增 13 个 pytest（prepare 6 + submit 4 + hash chain hook 3），全量 107 通过。prepare 不写 token → git commit 被 hook 阻塞；旧 exploit（spec-vc commit 无子命令拿 token）已封堵。SPEC_VC_BYPASS 保留为 raw escape。首次完整 dogfooding prepare/submit 流程提交。
- **Pre-Validation**: 基线94个pytest全部通过。漏洞复现成功：spec-vc commit 2>/dev/null > /dev/null 生成token后，git commit -m 'test: exploit reproduction [ADR-008]' 直接通过且创建commit——token仅证明命令被调用过，不证明审计子流程完成。此commit已通过git reset --soft撤销并清理。当前工作区干净，Spec-003三个形式化文件已生成，spec check全部就绪。
- **Post-Validation**: 改后验证：(1) 全量 107 个 pytest 通过（基线 94 + 新增 13：test_commit_prepare 6 个 + test_commit_submit 4 个 + test_cli hash chain 3 个）。(2) prepare 不写 token——spec-vc commit prepare 后 .git/spec-vc-commit-token 不存在，直接 git commit 被 hook 阻塞输出'未找到提交 token。请通过 spec-vc commit prepare + submit 流程提交代码'。(3) 旧 exploit（spec-vc commit 无子命令生成 token 后 git commit）不再有效——commit 无子命令时返回 1 提示指定子命令。(4) hook hash chain 校验：合法 token（5行含3哈希）匹配时通过并消费；篡改 audit-report 后哈希不匹配阻塞输出'不匹配'。(5) SPEC_VC_BYPASS 保留为 raw escape——设置后无 token 仍可 git commit 且写 bypass 日志。(6) 错误提示信息已更新为 prepare + submit 流程指引。
## References

- **Related ADRs**: <!-- ADR-XXX (supersedes / is superseded by / relates to) -->
- **Commits**: f196333 feat(commit): 引入 TTY-bound prepare/submit 两阶段提交流程 [ADR-008]
- **Specs**: <!-- 未来由 spec-vc 填充:引用 specs/ 下的契约单元 + 版本 -->
- **External**: <!-- 论文、RFC、issue、讨论链接 -->

---

<!--
Status 状态机:
  Proposed        ← 初始,提交后等待讨论
  Accepted        ← 决策生效,代码可开始实现
  Deprecated      ← 不再使用但未被替代
  Superseded by ADR-XXX  ← 被新决策替代,保留历史

更改状态时不要删除旧记录,在本文件顶部 Status 字段更新即可。
-->
