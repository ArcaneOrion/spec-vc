# ADR-012: 门禁消息增强：失败时返回可执行指引而非仅阻塞

- **Date**: 2026-05-08
- **Status**: Proposed
- **Deciders**: arcaneorion
- **Tags**: commit-protocol, ux, ai-agent

## Context

ADR-011 简化了提交流程，将防护收拢到 commit-msg hook。但实际实施 ADR-011 时暴露了两个流程断点：

1. **改代码之前没有有效门禁**：`change validate --phase pre` 只检查全局 Spec 就绪状态，不检查 ADR 是否有关联 Spec、clarify 是否完成。AI 可以跳过 Spec 创作协议直接进入代码修改。
2. **门禁阻塞消息只有"不行"没有"怎么做"**：commit-msg hook 和 `validate --phase pre` 的错误消息是功能性描述（"未找到 subagent 审计记录"、"stage 为 clarify"），AI 读到后知道出了问题，但不知道正确的下一步该做什么。

这两个断点的根因相同：门禁设计假设使用者是人类（读完错误消息后自行判断下一步），但实际主要使用者是 AI Agent——它需要明确的可执行指令来形成正确行为意图。

## Decision

1. 所有门禁阻塞消息增加"下一步"指引，包含具体的 CLI 命令和操作步骤
2. 所有门禁阻塞消息末尾追加 `详细流程请查看 SKILL.md 检查正确流程`
3. `change validate --phase pre` 增加 clarify 完整性检查（stage 仍为 discover/clarify 时阻塞）和 ADR→Spec 关联检查（ADR 无关联 Spec 且变更涉及代码路径时提示）
4. `commit prepare` 的输出消息从"请运行 submit"改为"请审计后直接 git commit"并列举 hook 校验项

## Consequences

- 积极后果：AI Agent 遇到门禁阻塞时能自动形成正确的补救行动意图，减少流程跳步
- 消极后果：错误消息变长，人类在终端看到可能显得冗余
- 中性后果：SKILL.md 成为错误消息的权威引用点，其内容需要保持与代码一致

## Alternatives Considered

- **PreToolUse hook 在 AI 修改文件时拦截**：在 AI 调用 Write/Edit 工具修改 src/ 下文件时检查流程状态。更主动但侵入性太强，且需要精确判断哪些文件修改需要门禁。
- **在 _active.md 中增加阶段完成标记**：让门禁检查更有结构化依据。但不改变消息内容的话仍只解决"检查是否通过"不解决"告诉 AI 该怎么做"。

## Implementation Plans

- **Plan**: doc/arch/plans/ADR-012-plan-001.md
- **Summary**: 完成 commit 协议简化（ADR-011 落地）+ 门禁消息可执行指引（ADR-012 plan-001 全部 8 个目标）：5 处错误消息追加分步指引和 SKILL.md 引用；change validate --phase pre 三项检查（clarify 完整性 / ADR-relevant Spec 就绪 / ADR→Spec 关联提示）；commit prepare 输出列举 4 项 hook 校验项；spec.has_associated_spec / spec.relevant_spec_issues 抽取领域辅助函数；adr.check_adr_continuity + adr_new 编号空洞警告；spec_new 编号与 ADR 对齐。pytest 78/78 通过。plan-002 留作设计草稿，未引入 _active.md 引用链。
- **Pre-Validation**: Spec-012 formalized。代码改动清单：1) hooks.py 5处消息增强+SKILL.md引用 2) cli.py commit prepare输出改写+validate pre增强 3) spec.py Spec编号与ADR对齐 4) adr.py 编号连续性检查 5) 测试更新+新增。72测试通过（编码号对齐+ADR连续性检查+plan stage 3个新增测试）。
- **Post-Validation**: ADR-012 plan-001 剩余 3 项实施完成。代码改动: (1) commit.py check_subagent_session 错误消息追加 Agent 工具指引 + spec-vc init 提示 + SKILL.md 引用; (2) hooks.py HELP_MISSING/HELP_SLOT/_check_plan_stage/_check_spec_readiness_for_adr 4 处错误消息追加可执行指引 + SKILL.md 引用，_check_spec_readiness_for_adr 改为调 spec.relevant_spec_issues 与 cli pre 检查口径对齐; (3) spec.py 新增 has_associated_spec/relevant_spec_issues 两个辅助函数; (4) cli.py cmd_change_validate pre 阶段新增 clarify 完整性检查（stage ∈ {discover,clarify} 且 missing fields 时阻塞）和 ADR→Spec 关联提示，Spec 检查范围从全局收窄为 ADR-relevant; cmd_commit_prepare 输出列举 4 项 hook 校验项 + SKILL.md 引用; (5) SKILL.md 同步 validate --phase pre 描述。测试: 新增 6 个用例（HELP_MISSING/SLOT 含 SKILL.md、commit prepare 列 4 项、clarify 阻塞、Spec 隔离、无 Spec 提示、hook 阻塞 Spec 不就绪含 SKILL.md），原有 4 个测试加 SKILL.md 断言。pytest 78/78 全过（之前 72）。手工验证: spec-vc spec check 全部就绪、change next-question 显示 stage=implement-ready。pyright 仅遗留与本次改动无关的 cmd_change_start/cmd_skill_load 中 dict.get 返回 object 的类型推断警告。
## References

- **Related ADRs**: ADR-011 (移除 commit submit 阶段), ADR-009 (PostToolUse hook)
- **Commits**: dd4f7df feat(spec-vc): 完成 commit 协议简化与门禁消息可执行指引 [ADR-012]
- **Specs**: 待补充