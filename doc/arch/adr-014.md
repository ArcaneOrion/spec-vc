# ADR-014: 修复审计发现的6个致命缺陷

- **Date**: 2026-05-14
- **Status**: Proposed
- **Deciders**: arcaneorion
- **Tags**: 

## Context

<!--
描述起作用的力量:技术的、政治的、社会的、项目局部的。
这些力量往往处于紧张状态,应明确指出。
语言保持价值中立,只陈述事实和约束,不预判决策方向。
-->

## Decision

<!--
我们将...(主动语态,完整句子)
只写最终选择,不写"可能"、"或许"。
-->

## Consequences

<!--
- 积极后果:
- 消极后果:
- 中性后果(被引入的新约束 / 新的学习成本 / 路径依赖):
-->

## Alternatives Considered

<!--
列出被排除的备选方案及其排除理由。
这部分对未来读者最有价值——帮他们理解决策边界。
-->

## Implementation Plans

- **Plan**: doc/arch/plans/ADR-014-plan-001.md
- **Summary**: 修复6个致命缺陷：(B1)删除change.py死代码导入消除导入遮蔽；(B2)删除create_plan重复Risks and Rollback模板段；(B3)config.py用isinstance类型守卫替换bool()/list()/int()强制转换防止静默数据损坏；(B4)read_plan_content加plan_id正则校验+plan_path加路径包含检查堵住路径遍历；(B5)hooks/commit-msg模板化init时动态填充SPEC_VC_BIN消除硬编码路径；(B6)check-refs.sh用索引数组+临时文件替代declare -A兼容Bash 3.2。全部89个测试通过。
- **Pre-Validation**: 变更范围已验证：6个致命缺陷定位明确，每项修复方案已做可行边界审计。B1纯删除无风险；B2行删除无副作用；B4路径校验覆盖19处plan_path调用需逐一验证；B3 isinstance守卫对正确配置零影响对错误配置明确报错；B5 hook模板替换在spec-vc init时动态计算路径；B6索引数组替代关联数组消除macOS兼容性问题。所有修复可独立回退。
- **Post-Validation**: 全部6个致命缺陷已修复，89个测试全部通过：(1)B1删除死代码导入，change子系统22测试通过；(2)B2删除重复模板段，change子系统22测试通过；(3)B4添加plan_id正则校验+plan_path路径包含检查，全测试套件89通过；(4)B3用isinstance类型守卫替换bool()/list()/int()强制转换，全测试套件89通过；(5)B5 hooks/commit-msg模板化+_install_hook动态填充+_init_claude_hook动态计算路径，全测试套件89通过；(6)B6 check-refs.sh用索引数组+临时文件替代declare -A，脚本在仓库内正确运行并识别出2个孤儿ADR
## References

- **Related ADRs**: <!-- ADR-XXX (supersedes / is superseded by / relates to) -->
- **Commits**: 待补充
- **Specs**: <!-- 未来由 spec-vc 填充:引用 specs/ 下的契约单元 + 版本 -->
- **External**: <!-- 论文、RFC、issue、讨论链接 -->

---

<!--
Status 状态机:
  Proposed        ← 初始,提交后等待讨论
  Accepted        ← 决策生效,代码可开始实现
  Deprecated      ← 不再使用但未被替代
  Superseded by ADR-XXX  ← 被新决策替代,保留历史

更改状态时不要删除旧记录,在本文件顶部 Status 字段更新即可。
-->
