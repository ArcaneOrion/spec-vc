# ADR-006: 引入 commit token 门禁机制，强制通过 spec-vc commit 提交

- **Date**: 2026-04-27
- **Status**: Proposed
- **Deciders**: arcaneorion
- **Tags**: 

## Context

<!--
描述起作用的力量:技术的、政治的、社会的、项目局部的。
这些力量往往处于紧张状态,应明确指出。
语言保持价值中立,只陈述事实和约束,不预判决策方向。
-->

## Decision

<!--
我们将...(主动语态,完整句子)
只写最终选择,不写"可能"、"或许"。
-->

## Consequences

<!--
- 积极后果:
- 消极后果:
- 中性后果(被引入的新约束 / 新的学习成本 / 路径依赖):
-->

## Alternatives Considered

<!--
列出被排除的备选方案及其排除理由。
这部分对未来读者最有价值——帮他们理解决策边界。
-->

## Implementation Plans

- **Plan**: doc/arch/plans/ADR-006-plan-001.md
- **Summary**: 在 commit-msg hook 中增加 token 校验逻辑: spec-vc commit 在 .git/spec-vc-commit-token 写入一次性 token (300s 过期)，commit-msg hook 校验 token 存在且未过期后消费。同步更新 SKILL.md 启动协议和 plan 阶段展示要求。新增 test_commit_msg_blocks_without_token 测试覆盖无 token 阻塞场景。全量 46 测试通过。
- **Pre-Validation**: 变更前验证：1) 当前 commit-msg hook 只校验 [ADR-NNN] 格式，不校验提交通路，git commit 可直接执行 2) SKILL.md plan 阶段无'展示计划内容到前台'要求，AI 只输出文件路径 3) 现有测试覆盖 hooks 格式校验场景但无 token 门禁测试
- **Post-Validation**: 变更后验证：1) 46/46 测试全部通过 2) 无 token 直接 git commit 被阻塞: '未找到提交 token。请通过 spec-vc commit 流程提交代码' 3) spec-vc commit 写入 token 后 git commit 成功 4) token 一次性消费，提交后自动删除
## References

- **Related ADRs**: <!-- ADR-XXX (supersedes / is superseded by / relates to) -->
If1f1e feat(commit): 引入 token 门禁机制，强制通过 spec-vc commit 提交 [ADR-006]
- **Specs**: <!-- 未来由 spec-vc 填充:引用 specs/ 下的契约单元 + 版本 -->
- **External**: <!-- 论文、RFC、issue、讨论链接 -->

---

<!--
Status 状态机:
  Proposed        ← 初始,提交后等待讨论
  Accepted        ← 决策生效,代码可开始实现
  Deprecated      ← 不再使用但未被替代
  Superseded by ADR-XXX  ← 被新决策替代,保留历史

更改状态时不要删除旧记录,在本文件顶部 Status 字段更新即可。
-->
